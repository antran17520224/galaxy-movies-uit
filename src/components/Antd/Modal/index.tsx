import * as React from "react";
import { Modal } from "antd";

interface IProps {

}

export const ModalConfirm: React.FC<IProps> = (props: IProps) => {
    return (
        <Modal>
            
        </Modal>
    )
}
